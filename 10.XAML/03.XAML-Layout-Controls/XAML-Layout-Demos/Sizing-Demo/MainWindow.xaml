<Window x:Class="Sizing_Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="340" Width="718">
    <Window.Resources>
        <Style TargetType="Border">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    <StackPanel Orientation="Horizontal" Background="LightBlue" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border>
            <Button Content="Some Text" Margin="25" Padding="0" />
        </Border>
        <Border>
            <Button Content="Some Text" Margin="20" Padding="5" />
        </Border>
        <Border>
            <Button Content="Some Text" Margin="15" Padding="10" />
        </Border>
        <Border>
            <Button Content="Some Text" Margin="10" Padding="15" />
        </Border>
        <Border>
            <Button Content="Some Text" Margin="5" Padding="20" />
        </Border>
        <Border>
            <Button Content="Some Text" Margin="0" Padding="25" />
        </Border>
    </StackPanel>
</Window>
